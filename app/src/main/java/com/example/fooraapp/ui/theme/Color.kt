package com.example.fooraapp.ui.theme

import androidx.compose.ui.graphics.Color

// ── Tông màu thương hiệu (Brand Identity) ──────────────────────
val BrandOrange   = Color(0xFFFF6B35) // Cam chính (Kích thích vị giác)
val BrandDeep     = Color(0xFFE84E0F) // Cam đậm (Dùng cho Gradient/Button)
val BrandYellow   = Color(0xFFFFC107) // Vàng (Dùng cho Rating/Star)

// ── Màu nền và bề mặt (Surface & Background) ──────────────────
val BgLight       = Color(0xFFFFF9F6) // Nền hồng cam cực nhạt (Tạo cảm giác sạch sẽ)
val SurfaceCard   = Color(0xFFFFFFFF) // Màu trắng cho các thẻ Card
val FieldBorder   = Color(0xFFE5E7EB) // Viền các ô nhập liệu

// ── Màu cho chữ (Typography) ──────────────────────────────────
val TextPrimary   = Color(0xFF1A1A2E) // Chữ chính (Đen xanh đậm)
val TextSecondary = Color(0xFF6B7280) // Chữ phụ (Xám)

// ── Màu trạng thái (Status) ────────────────────────────────────
val ErrorRed      = Color(0xFFDC2626)
val SuccessGreen  = Color(0xFF10B981)

// ── Tương thích với mặc định của Android Studio ───────────────
val Purple80      = Color(0xFFFFAB91)
val PurpleGrey80  = Color(0xFFCCC2DC)
val Pink80        = Color(0xFFEFB8C8)

val Purple40      = BrandOrange
val PurpleGrey40  = Color(0xFF625B71)
val Pink40        = Color(0xFF7D5260)